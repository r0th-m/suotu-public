# 路径穿越:../ 序列(含编码变体)与敏感系统文件
# target=any:raw 兜底匹配,raw_t0 等未归一源也能吃到这条(target=any 语义)
id: path-traversal
title: 路径穿越/敏感系统文件访问
severity: high
target: any
match:
  raw:
    - "../"
    - "..%2f"
    - "..\\"
    - "/etc/passwd"
    - "/etc/shadow"
    - "boot.ini"
    - "win.ini"
note: >-
  目录穿越标志(../ 与 %2f 编码、反斜杠变体)及 Unix/Windows 敏感文件
  (/etc/passwd、boot.ini 等)是 OWASP 路径穿越测试的公开标志物。
  用 raw 整行兜底:路径/查询任何一段出现即命中,不依赖归一字段齐全。
